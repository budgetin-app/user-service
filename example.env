# Budgetin Project
# Environment Configuration for User Management Service

# App configuration (APP_ENV:production/local)
APP_ENV=local
APP_DEBUGABLE=true

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=user_management_db
DB_USER=postgres
DB_PASSWORD=postgres

# Server configuration
SERVER_IP=localhost
SERVER_PORT=8080

# Logging (DEBUG/TRACE/INFO)
LOG_LEVEL=DEBUG

# Hash configuration
PASSWORD_HASH_ALGORITHM=bcrypt

# SMTP configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SENDER_EMAIL=example@email.com
SMTP_SENDER_NAME=Sender
SMTP_SENDER_PASS=examplepassword